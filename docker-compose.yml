# --- CONFIGURANDO O postgreSQL NO DOCKER --- #
services:
  database_test:
    image: postgres:15
    container_name: dockerTest
    environment:
      - POSTGRES_DB=test
      - POSTGRES_USER=test
      - POSTGRES_PASSWORD=senha1234
    ports:
      - "5433:5432"